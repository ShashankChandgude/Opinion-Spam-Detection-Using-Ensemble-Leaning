[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
addopts = --strict-markers --cov=src --cov-report=term-missing --cov-report=xml
markers =
    slow: mark slow tests to exclude with '-m "not slow"'